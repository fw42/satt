README
* Instances of which classes are not serializable?
* Instances of which classes are stored with reference id?

TODO
* Flags support (if supported by Marshal). Put in docs.
* If no ivars, don't store empty hash
* Test with ActiveRecord
* Package as gem
* Benchmark speed / size
